todo

make a readme for prof
rename the schema files
expand: org groups (not just single assignee)
organization - upgr
expand: add listeners to config
events to config
participans
global (engine) variables dictionary
camunda listener after = opration in bonita
process initiator (add to both bonita and camunda)

forms:
    make example with req lenth

preruntime check if a certain variable is going to appear bofore some task that requires starts (e.g. a worning) (nice to have)

timer events only fixed and duration (bonita limitation) + validate that from bpmn diagram



notes:
in case of expressions, if can be resolved before runtime, do it. if not, leave as is.
everything ,ust have a name (rule for modeling)
